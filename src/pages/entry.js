import { motion } from 'framer-motion';
import React from 'react';
import EntryPage from '../modules/Entry/Entry';
import { variants } from '../animations/fadeOut';


const Entry = () => {
    return( 
        <EntryPage />
    );
};

export default Entry;